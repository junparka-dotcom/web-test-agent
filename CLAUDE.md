# 프로젝트: web-test-agent

## 목적
AI 기반 웹/앱 개발환경 프로젝트의 차별화 기능 실험. 코드를 짜는 AI가 아니라,
**완성된 웹 페이지를 AI 에이전트가 직접 클릭/입력해보며 요소들이 정상 작동하는지 점검하는 도구**를 만드는 중.

## 에이전트의 정체성 (중요)
이 에이전트는 "목표를 끝까지 완수하는 봇"이 아니라 **"QA 감사관"**이다.
- 목적: 페이지의 상호작용 요소(버튼/링크/폼/애니메이션)가 정상 작동하는지 하나씩 점검하고 기록
- 목적이 아닌 것: 로그인 같은 특정 작업을 완료하는 것 자체

## 아키텍처
1. **관찰**: `page.ariaSnapshot({ mode: 'ai' })` — 접근성 트리를 YAML 텍스트로 추출 (요소마다 [ref=eN] 포함)
2. **판단**: Claude API에 스냅샷 + 임무를 보내고, tool use로 다음 행동 하나를 구조화된 형식으로 받음
3. **행동**: 받은 ref로 `page.locator('aria-ref=eN')`를 통해 실제 클릭/입력/호버/드래그/파일업로드 등 실행
4. **확인**: 행동 후 다시 스냅샷 찍어서 다음 판단에 사용, 반복

페이지 한 장을 점검하는 이 루프는 `pageAuditor.js`의 `auditPage()` 함수 하나로 모여 있고,
`agent.js`(페이지 1장)와 `crawler.js`(사이트 여러 페이지)가 이 함수를 호출하는 얇은 진입점이다.

## 핵심 설계 규칙 (시행착오로 얻은 결론)
- **인증 화면은 절대 자동 통과 금지**: 로그인/결제/CAPTCHA 등을 만나면 `request_human_help` 호출.
  화면에 계정 정보가 적혀 있어도 무시하고 사람에게 물어봐야 함 (AI가 "안내문에 적혀있으니 추측이 아니다"라고
  스스로 합리화하는 허점이 있었음 — 규칙에 "화면에 적혀있어도 무시하라"를 명시해야 막힘).
- **HTTP Basic Auth는 폼 로그인과 다른 방식으로 막아야 함**: DOM이 뜨기도 전에 `page.goto` 자체가
  `ERR_INVALID_AUTH_CREDENTIALS`로 실패해서, AI가 `request_human_help`를 호출할 기회조차 없다.
  그래서 `pageAuditor.js`의 `ensureReachable()`이 이 에러를 코드 레벨에서 감지해서, 자격 증명을 코드에
  하드코딩하지 않고 터미널로 사람에게 직접 물어본 뒤 `Authorization` 헤더로 재시도한다. 이 페이지 점검이
  끝나면 헤더는 항상 지워서 다른 페이지/외부 사이트로 자격증명이 새지 않게 한다.
- **파괴적 행동(로그아웃 등)은 프롬프트 지시만으로 막지 말고 코드 레벨에서 이중 확인**:
  AI 판단과 별개로 코드가 클릭 직전 ref의 이름에 logout/로그아웃이 포함되는지 검사해서, 클릭 직후 강제 종료
  (AI 혼자 순서를 착각해도 안전망 역할). 다만 처음엔 그냥 조용히 끊어버려서 로그아웃이 실제로 성공했는지
  기록도, 최종 요약도 안 남는 문제가 있었음 — 지금은 강제 종료는 그대로 유지하되, 종료되는 그 순간에
  코드가 직접(AI 판단 없이) 로그인 폼이 다시 나타났는지 검증해서 findings에 기록하고 요약까지 자동 생성한다.
  ("안전망은 유지하면서 기록은 남기는" 절충안.)
- **클릭이 다른 페이지로 이동시키면 항상 원래 점검 중이던 페이지로 복귀**: 처음엔 외부 도메인일 때만
  복귀시켰는데, 내부 링크(같은 사이트의 다른 페이지)를 클릭해도 그냥 그 페이지에 머물러버려서 한 번의
  페이지 점검이 사이트 전체를 정처 없이 돌아다니는 문제가 있었음. 지금은 내부/외부 상관없이 최초 로드된
  URL(`startUrl`)과 달라지면 무조건 되돌아온다 — 사이트 전체 탐색은 `crawler.js`가 별도로 담당하므로,
  페이지 하나의 점검은 그 페이지 안에서만 이뤄져야 함.
- **개별 행동 하나의 실패가 페이지 점검 전체를 죽이면 안 됨**: `<select>`를 `fill_element`로 채우려다
  크래시하는 것처럼, 행동 하나가 예상 밖으로 실패할 수 있다. 각 tool 실행을 try/catch로 감싸서 실패 사유를
  다음 턴 AI에게 "실행 실패: ..."로 돌려주고, AI가 다른 방식을 시도하거나 issue로 기록하고 넘어가게 한다.
- **alert/confirm/prompt는 리스너 없이 두면 안 됨**: Playwright는 `page.on('dialog', ...)` 리스너가
  없으면 대화상자를 조용히 자동 취소해버려서, AI는 그게 떴는지조차 모른다. 항상 자동으로 "확인" 처리하고,
  감지된 내용은 다음 턴에 알려준다.
- **외부 링크는 스킵하지 말고 실제로 클릭해서 검증**: href 형식만 보는 건 진짜 검증이 아님(깨진 링크 못 잡음).
  대신 코드가 새 탭 열림/도메인 변경을 감지해서 자동으로 원래 페이지로 복귀시켜, 사람 개입 없이 처리.
- **한 턴에 tool_use가 여러 개 올 수 있음**: `response.content.find()`가 아니라 `filter()`로 전부 처리하고,
  각각에 대해 반드시 `tool_result`를 빠짐없이 반환해야 함 (하나라도 빠뜨리면 다음 호출에서 400 에러 발생).
- **대화 히스토리를 유지해야 함**: 매번 새 대화로 호출하면 AI가 이전에 뭘 확인했는지 기억 못 해서
  "다른 거 다 확인하고 로그아웃은 마지막에" 같은 순서 지시가 무의미해짐. `messages` 배열을 계속 누적해서 전송.
- **모델은 상황에 맞게**: 개발/테스트 중엔 `claude-haiku-4-5-20251001` (저렴, 이 정도 판단엔 충분).
  더 복잡한 페이지에서 정확도 부족하면 `claude-sonnet-5`로 전환 고려.
- **크롤러는 페이지 단위로 실패를 격리해야 함**: `/basic_auth`처럼 `page.goto` 자체가 실패하는 페이지가
  섞여 있어도 나머지 페이지 점검은 계속돼야 하므로, `crawler.js`는 `auditPage()` 호출을 페이지마다
  try/catch로 감싼다. 이전 페이지의 실패한 goto가 남긴 상태 때문에 바로 다음 페이지의 goto가 연쇄적으로
  "다른 내비게이션에 의해 중단됨" 에러를 내는 경우가 있었는데, 이건 크롤러가 아니라 `pageAuditor.js`의
  `gotoWithRetry()`가 그 에러 패턴에 한해 재시도로 흡수한다.
- **내부 링크 발견은 정적 스캔 + 실제 클릭 둘 다 필요**: `<a href>`만 긁으면 React 같은 SPA에서 흔한,
  href 없이 `onClick`으로 `history.pushState`/라우터를 호출하는 네비게이션은 놓친다. 그런데 AI가 그런
  버튼을 눌러서 점검하는 순간 `page.url()`이 바뀌는 건 이미 감지하고 있었으므로(원래 페이지로 복귀하는
  로직), 그 시점에 발견한 URL을 `auditPage`가 `discoveredLinks`로 같이 반환하고 `crawler.js`가
  정적 스캔 결과와 합쳐서 큐에 넣는다. 단, 그 페이지에서 AI가 실제로 클릭해본 요소에 한해서만 발견되므로
  (규칙 4의 효율화로 일부 요소를 건너뛸 수 있음) 정적 `<a href>` 스캔을 대체하지 않고 보완하는 용도다.
- **URL 정규화로 큐 중복 방지**: `/page`와 `/page/`(trailing slash), `?a=1&b=2`와 `?b=2&a=1`(쿼리
  순서)처럼 사실상 같은 페이지가 다른 문자열이라는 이유로 크롤러 큐에 중복으로 들어갈 수 있었다.
  `linkUtils.js`의 `normalizeUrl()`이 해시 제거 + trailing slash 제거(루트 `/`는 제외) + 쿼리 파라미터
  정렬을 해주고, 정적 스캔(`extractInternalLinks`)과 클릭으로 발견한 링크(`discoveredLinks`), 크롤러의
  시작 URL 모두 이 함수를 거쳐서 큐/visited 집합에 들어간다. 정규화된 URL로 그냥 navigate해도, the-internet
  같은 사이트는 trailing slash 유무를 서로 리다이렉트해주므로 실제 로드되는 페이지는 동일하다.
- **API를 직접 호출하는 코드와 순수 로직은 분리해서, 순수 로직만 API 키 없이 테스트 가능해야 함**:
  `pageAuditor.js`가 모듈 로드 시점에 `new Anthropic()`을 실행하면, API 키가 없는 환경에서는
  `require`만 해도 죽어버려서 `isLogoutRef`/`formatFindings`/`gotoWithRetry` 같은 순수 함수조차
  테스트할 수 없었다. `getAnthropic()`으로 실제 호출 시점까지 클라이언트 생성을 미뤄서 해결함.
  같은 이유로 `crawler.js`의 링크 추출 로직은 즉시 실행되는 IIFE와 분리해서 `linkUtils.js`로 뺐다
  (그래야 `require`만 해도 브라우저가 뜨는 일이 없다).
- **API 크레딧 비용에 유의**: `agent.js`/`crawler.js`를 실제로 실행하면 Claude API를 스텝마다 호출해서
  비용이 발생한다. 개발 중 코드를 고칠 때마다 라이브로 재실행해서 검증하지 말고, 정적 검사
  (`node --check`, `npm run test:unit`)로 먼저 확인하고, 실제 API를 호출하는 실행은 사람이 명시적으로
  요청할 때만 한다.

## 파일 구조
- `pageAuditor.js` — **핵심 파일**. 페이지 한 장을 점검하는 루프(`auditPage`)와 그 안에서 쓰는
  안전장치(로그아웃 안전망, Basic Auth 처리, goto 재시도, dialog 자동 처리)가 전부 여기 있음
- `agent.js` — 단일 페이지(현재는 `/secure`) 점검용 얇은 진입점, `auditPage`를 한 번 호출
- `crawler.js` — 사이트 전체를 큐 기반으로 도는 크롤러. `auditPage`를 페이지마다 반복 호출하고
  페이지 단위로 실패를 격리, 결과를 `crawl-report.json`에 저장
- `linkUtils.js` — 크롤러가 쓰는 내부 링크 추출 순수 함수 (`extractInternalLinks`)
- `explore.js` — 초기 실험용 스크립트 (접근성 트리 추출 테스트)
- `check-env.js` — .env 로딩 확인용
- `tests/unit/` — **API를 호출하지 않는** 순수 로직 테스트 (`npm run test:unit`으로 실행, 비용 없음)
- `tests/example.spec.js` — Playwright 설치 확인용 플레이스홀더 (에이전트 로직과 무관)
- `crawl-report.json` — 크롤러 실행 결과물 (매 실행마다 새로 생성됨, `.gitignore`에 등록, 소스 아님)
- `.env` — ANTHROPIC_API_KEY 보관 (.gitignore에 등록됨, 커밋 금지)

## 현재 상태 / 다음 작업
**완료**
- 로그인 → 다른 요소들 점검 → 로그아웃(마지막, 코드 안전망 + 자동 기록) 순서 흐름 검증됨
- 외부/내부 링크 클릭 후 원래 페이지 복귀 로직
- 드롭다운(select_option), 호버(hover_element), 드래그앤드롭(drag_element), 파일 업로드(upload_file),
  브라우저 대화상자(alert/confirm/prompt) 자동 처리 지원
- HTTP Basic Auth 벽 처리 (사람에게 직접 자격증명 입력받음)
- 크롤러 레이어 (`crawler.js`) — 내부 링크 큐잉(정적 `<a href>` 스캔 + 클릭으로 발견한 SPA 라우팅 링크),
  페이지 단위 실패 격리, goto 재시도
- API 호출 없는 순수 로직 유닛 테스트 (`tests/unit/`, 18개)

**남은 것**
- **라이브 통합 실행 검증**: Basic Auth 입력 흐름, dialog 처리, hover/drag/upload를 실제 API로 한 번에
  돌려서 확인하는 작업이 아직 안 됨 — API 크레딧 비용 때문에 개발이 더 진행된 뒤 한 번에 확인하기로 함.

## 프로젝트 배경 (큰 그림)
이 저장소(web-test-agent)는 더 큰 프로젝트의 한 부분이다:
**"AI 기반 웹/앱 개발환경"** — AI에게 프롬프트로 설명해서 코드를 짜게 하는(바이브 코딩) 개발 도구를 만드는 것이 최종 목표.
그 도구의 차별점으로, 코드 작성 AI와는 별개로 **완성된 결과물을 직접 클릭/입력해보며 검증하는 AI 에이전트**를
통합하려는 것이고, 이 저장소는 그 검증 에이전트만 따로 떼어내 프로토타입으로 만드는 중이다.

**현재 https://the-internet.herokuapp.com 로 테스트하는 건 어디까지나 개발/디버깅용 임시 대상이다.**
최종적으로는 이 에이전트가 특정 사이트 하나가 아니라, 코딩 AI가 그때그때 만들어낸 **임의의 웹 페이지**를
대상으로 동작해야 한다. 즉 URL이나 페이지 구조를 하드코딩하는 방향이 아니라, 어떤 페이지가 주어져도
접근성 트리만 보고 알아서 점검할 수 있는 범용성을 목표로 설계 방향을 유지해야 한다.
